/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['farm53848626551.staticflickr.com'], // Ajoutez le nom d'hôte de votre source d'images
    },
  };
  
  export default nextConfig;